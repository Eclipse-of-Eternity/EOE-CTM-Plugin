package ctm.mc.eoe.scoreboardapi;

import org.bukkit.Bukkit;
import org.bukkit.entity.Player;

import java.util.Objects;

public class ScoreboardAPI {
    public static void setScore(Player player, String sbName, int value){
//        Objects.requireNonNull(Bukkit.getScoreboardManager().getMainScoreboard().getObjective(sbName)).getScoreFor(player).setScore(value);
//        LogFilter.canceling = true;
//        Bukkit.getConsoleSender().sendMessage(
//                String.format("/scoreboard players set %s %s %s", player.getName(), sbName, value)
//        );
//        LogFilter.canceling = false;
    }
}
